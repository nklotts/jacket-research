# 🎽 Adversarial Jacket Training System

Система обучения adversarial паттернов для кофты с LED диодами, которая обманывает YOLO детекцию.

## 📋 Описание

Система состоит из:
- **ПК с обучающей моделью** - где происходит RL обучение (SAC алгоритм)
- **Raspberry Pi** - контролирует камеры окружающей среды и LED дисплей
- **Камера на ПК** - снимает человека в кофте для YOLO детекции

### Как это работает:

1. **Raspberry Pi** захватывает 2 изображения окружающей среды → отправляет на ПК
2. **ПК** получает изображения → нейросеть генерирует LED паттерн → отправляет на RPi
3. **RPi** отрисовывает паттерн на LED диодах → отправляет подтверждение
4. **ПК** захватывает изображение человека в кофте → подает в YOLO
5. **YOLO** определяет вероятность обнаружения человека
6. **RL агент** получает reward = -вероятность детекции → обучается

## 🎮 Использование

### 1. Конфигурация

Отредактируйте `config.py` под ваши нужды:

```python
class TrainingConfig:
    # Адрес Raspberry Pi
    SERVER_HOST = '192.168.1.100'  # IP вашего RPi
    SERVER_PORT = 5000
    
    # Параметры обучения
    NUM_EPISODES = 1000
    LEARNING_STARTS = 100
    SAVE_MODEL_EVERY = 100
    
    # Камера ПК
    PC_CAMERA_INDEX = 0  # 0 = встроенная, 1 = USB и т.д.
    
    # Сохранение датасета
    SAVE_DATASET = True
    DATASET_DIR = './dataset'
```

### 2. Запуск сервера (Raspberry Pi)

На Raspberry Pi запустите:
```bash
python server.py
```

Для **ТЕСТИРОВАНИЯ** на одном ПК (симуляция RPi):
```bash
python server.py
```

### 3. Запуск обучения (ПК)

```bash
python train.py
```