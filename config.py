"""
Конфигурация обучения для оптимизации adversarial LED-паттернов.
Все параметры эксперимента определяются здесь.

Алгоритм: SimbaV2 + distributional SAC (перенесено из paper.py).
"""


class TrainingConfig:

    # =========================================================================
    # СЕТЬ
    # =========================================================================
    SERVER_HOST = '127.0.0.1'
    SERVER_PORT = 5000

    # =========================================================================
    # КАМЕРА ПК
    # =========================================================================
    PC_CAMERA_INDEX    = 0
    CAMERA_WARMUP_TIME = 2.0   # секунд

    # =========================================================================
    # YOLO
    # =========================================================================
    YOLO_MODEL          = 'yolov8n.pt'
    YOLO_CONF_THRESHOLD = 0.55
    TARGET_CLASS        = 0    # класс "человек" в COCO

    # =========================================================================
    # ДИОДЫ / ПРОСТРАНСТВО ДЕЙСТВИЙ
    # =========================================================================
    BOARD_H  = 16
    BOARD_W  = 16
    N_BOARDS = 37
    # Итого диодов: BOARD_H * BOARD_W * N_BOARDS = 9472

    # Размер суперпикселя: одно RGB-значение покрывает SUPERPIXEL_SIZE×SUPERPIXEL_SIZE диодов.
    # Должен нацело делить BOARD_H и BOARD_W. Допустимые значения: 1, 2, 4, 8, 16.
    SUPERPIXEL_SIZE = 4

    MAX_BRIGHTNESS = 10   # максимальная яркость диода (0–255)

    # =========================================================================
    # СОСТОЯНИЕ (STATE)
    # state_dim = 12  (3 канала × 2 статистики × 2 камеры)
    # =========================================================================
    IMAGE_SIZE = 64

    # =========================================================================
    # ШУМ ДЛЯ ИССЛЕДОВАНИЯ
    # Применяется ПОВЕРХ стохастической политики SAC.
    # Шум на действие — в tanh-пространстве [-1, 1].
    # =========================================================================
    STATE_NOISE_STD  = 0.05
    ACTION_NOISE_STD = 0.02

    # =========================================================================
    # ФУНКЦИЯ НАГРАДЫ
    # reward = REWARD_A * (DETECTION_THRESHOLD - детекция)
    #        + REWARD_B * дисперсия_действия
    #        + REWARD_C * отличие_от_предыдущего_действия
    # =========================================================================
    DETECTION_THRESHOLD  = 0.9
    REWARD_A             = 1e1
    REWARD_B             = 1e2
    REWARD_C             = 1e1
    DETECTION_AVG_FRAMES = 3

    # =========================================================================
    # ГИПЕРПАРАМЕТРЫ SIMBAV2 SAC (paper.py)
    # =========================================================================
    GAMMA       = 0.99
    TAU         = 0.005
    BATCH_SIZE  = 256       # paper: 256
    BUFFER_SIZE = 1_000_000

    # Distributional critic (C51) — Section 4.3
    NATOMS = 101
    VMIN   = -5.0
    VMAX   = 5.0

    # Размеры сетей — Section 4.4, Table 3
    ACTOR_D   = 128   # размер скрытого слоя актора
    ACTOR_NB  = 1     # число residual блоков в акторе
    CRITIC_D  = 512   # размер скрытого слоя критика
    CRITIC_NB = 2     # число residual блоков в критике

    # Скорость обучения с линейным декаем — Section 5.1, Table 3
    LR       = 1e-4
    LR_FINAL = 3e-5

    # Update-to-data ratio — обновлений критика на один env-шаг
    UTD = 6

    # Параметр сдвига для входа энкодера (Section 4.1, Eq.9)
    CSHIFT = 3.0

    # Сколько шагов делать чисто случайных действий до начала обучения.
    # Должно быть >= BATCH_SIZE.
    WARMUP_STEPS = 256

    # =========================================================================
    # ПАРАМЕТРЫ ОБУЧЕНИЯ
    # =========================================================================
    NUM_EPISODES          = 5000
    MAX_STEPS_PER_EPISODE = 1
    DEVICE                = 'cuda'
    VERBOSE               = True

    # =========================================================================
    # СОХРАНЕНИЕ
    # =========================================================================
    MODELS_DIR         = './checkpoints'
    LOGS_DIR           = './logs'
    DATASET_DIR        = './dataset'
    SAVE_MODEL_EVERY   = 1000
    SAVE_DATASET       = False
    SAVE_VISUALIZATION = False

    # =========================================================================
    # ТАЙМАУТЫ СОКЕТОВ
    # =========================================================================
    SOCKET_TIMEOUT         = 10
    PATTERN_RENDER_TIMEOUT = 60

    # =========================================================================
    # ВИЗУАЛИЗАЦИЯ
    # =========================================================================
    SHOW_YOLO_DETECTIONS = True
    SHOW_LED_PATTERN     = True
    SHOW_ENV_IMAGES      = True
    VISUALIZATION_DELAY  = 1

    WINDOW_POSITIONS = {
        'YOLO Detection - Adversarial Jacket': (0,   0),
        'LED Pattern':                          (700, 0),
        'RPi Camera 1':                         (0,   550),
        'RPi Camera 2':                         (700, 550),
    }
